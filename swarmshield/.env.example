# -----------------------------------------------------------------------
# SwarmShield environment template
# Copy this file to .env and fill in real values.
# -----------------------------------------------------------------------

# LLM Configuration (optional)
# Set XAI_API_KEY to enable Grok (xAI) enrichment/validation via LLMClient.
# Agents degrade gracefully when this is absent; all algorithmic features work without it.
XAI_API_KEY=your_xai_api_key_here
LLM_MODEL=grok-2-1212
LLM_TEMPERATURE=0.0

# Responder service configuration
# These values are used when running responder.py as a service.
COORDINATOR_IP=192.168.1.100
HONEYPOT_IP=192.168.1.99
RESPONDER_PORT=5003
RESPONDER_ID=responder-1

# Auto-unblock window for the Responder background loop.
# AUTO_UNBLOCK_SECONDS takes priority over AUTO_UNBLOCK_MINUTES; default is 300 (5 min).
AUTO_UNBLOCK_SECONDS=300
# AUTO_UNBLOCK_MINUTES=5
